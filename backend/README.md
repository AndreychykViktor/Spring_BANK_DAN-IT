# 🏦 Banking Application - Повний Гід

Повнофункціональний банківський додаток з Spring Boot бекендом та HTML фронтендом. Включає систему авторизації, управління користувачами, банківські операції, адмін панель, логування та WebSocket повідомлення.

## ✨ Функціонал

- **🔐 Система авторизації**: JWT токени, реєстрація та вхід
- **👥 Управління користувачами**: ролі (USER, ADMIN), адмін може призначати ролі
- **💳 Банківські операції**: створення акаунтів, поповнення, зняття, перекази
- **📊 Історія транзакцій**: повний запис всіх операцій
- **🎨 HTML інтерфейс**: готові сторінки для всіх операцій
- **🔒 Безпека**: Spring Security, шифрування паролів
- **📝 Логування**: детальне логування всіх операцій
- **🔌 WebSocket**: повідомлення про зміни рахунків в реальному часі
- **🧪 Тестування**: модульні тести для контролерів та сервісів

## 🛠 Технології

- **Backend**: Spring Boot 3.1.4, Spring Data JPA, Spring Security, WebSocket
- **База даних**: PostgreSQL
- **Frontend**: HTML, CSS, JavaScript (статичні файли)
- **Авторизація**: JWT токени
- **Логування**: SLF4J + Logback
- **Тестування**: JUnit 5, Mockito, H2 Database
- **Інструменти**: Maven, Docker Compose, Lombok

## 🚀 Як запустити

### Передумови
- Java 17 або вище
- Maven 3.6+
- Docker та Docker Compose
- Git

### Крок 1: Клонування репозиторію
```bash
git clone <repository-url>
cd Spring_BANK_DAN-IT
```

### Крок 2: Запуск бази даних PostgreSQL
```bash
cd backend
docker-compose up -d
```

**Перевірте, що Docker запущений!** Якщо бачите помилку про Docker daemon, запустіть Docker Desktop.

### Крок 3: Запуск Spring Boot додатку
```bash
mvn spring-boot:run
```

### Крок 4: Відкрити в браузері
- **Головна сторінка**: `http://localhost:9000/index.html`
- **Вхід**: `http://localhost:9000/login.html`
- **Реєстрація**: `http://localhost:9000/registration.html`
- **Користувацька панель**: `http://localhost:9000/user-dashboard.html`
- **Адмін панель**: `http://localhost:9000/admin-dashboard.html`
- **Тест API**: `http://localhost:9000/test.html`
- **WebSocket тест**: `http://localhost:9000/websocket-test.html`

## 🎯 Як користуватися

### 1. Реєстрація нового користувача
1. Відкрийте `http://localhost:9000/registration.html`
2. Заповніть форму реєстрації:
   - **Username**: унікальне ім'я користувача
   - **Email**: дійсна email адреса
   - **Password**: пароль (мінімум 6 символів)
3. Натисніть "Зареєструватися"
4. Після успішної реєстрації ви будете перенаправлені на сторінку входу

### 2. Вхід в систему
1. Відкрийте `http://localhost:9000/login.html`
2. Введіть логін та пароль
3. Натисніть "Увійти"
4. Після успішного входу ви будете перенаправлені на відповідну панель:
   - **USER** → `user-dashboard.html`
   - **ADMIN** → `admin-dashboard.html`

### 3. Користувацька панель (USER)
Доступні операції:
- **Перегляд рахунків**: список всіх ваших рахунків
- **Створення рахунку**: створення нового рахунку з валютою
- **Поповнення**: поповнення рахунку
- **Зняття коштів**: зняття коштів з рахунку
- **Переказ**: переказ коштів між рахунками
- **Історія транзакцій**: перегляд всіх операцій

### 4. Адмін панель (ADMIN)
Додаткові можливості:
- **Управління користувачами**: перегляд всіх користувачів
- **Призначення ролей**: зміна ролей користувачів
- **Системна інформація**: статистика додатку

### 5. WebSocket повідомлення
1. Відкрийте `http://localhost:9000/websocket-test.html`
2. Натисніть "Connect" для підключення до WebSocket
3. Виконайте операції з рахунками через інші сторінки
4. Спостерігайте за повідомленнями в реальному часі:
   - Поповнення рахунку
   - Зняття коштів
   - Перекази між рахунками

## 🔧 API Endpoints

### Авторизація
- `POST /auth/register` - Реєстрація користувача
- `POST /auth/login` - Вхід в систему

### Користувачі
- `GET /api/customers` - Список всіх користувачів
- `GET /api/customers/{id}` - Отримати користувача за ID
- `POST /api/customers` - Створити користувача
- `PUT /api/customers/{id}` - Оновити користувача
- `DELETE /api/customers/{id}` - Видалити користувача

### Рахунки
- `POST /api/accounts/{accountNumber}/deposit` - Поповнити рахунок
- `POST /api/accounts/{accountNumber}/withdraw` - Зняти кошти
- `POST /api/accounts/transfer` - Переказ між рахунками
- `GET /api/accounts/{accountNumber}` - Отримати інформацію про рахунок

### Транзакції
- `GET /api/transactions` - Список транзакцій
- `GET /api/transactions/{id}` - Отримати транзакцію за ID

### Адмін операції
- `GET /api/admin/users` - Список всіх користувачів (тільки ADMIN)
- `PUT /api/admin/users/{id}/role` - Змінити роль користувача (тільки ADMIN)

## 🔒 Безпека

### JWT Токени
- Токени дійсні 1 годину
- Автоматичне оновлення при активності
- Безпечне зберігання в localStorage

### Ролі користувачів
- **USER**: звичайний користувач, може управляти своїми рахунками
- **ADMIN**: адміністратор, має доступ до всіх функцій

### Шифрування
- Паролі шифруються за допомогою BCrypt
- Всі API запити потребують авторизації (крім `/auth/**`)

## 📝 Логування

Додаток веде детальні логи всіх операцій:

### Рівні логування
- **INFO**: звичайні операції (вхід, реєстрація, операції з рахунками)
- **WARN**: попередження (невдалі спроби входу, невалідні дані)
- **ERROR**: критичні помилки (системні збої)

### Приклади логів
```
INFO  - User 'john_doe' logged in successfully
INFO  - Deposit request for account 123456789: amount 100.0
WARN  - Login failed for user 'invalid_user': Bad credentials
ERROR - Registration failed for user 'test': Database connection error
```

## 🔌 WebSocket

### Підключення
```javascript
const socket = new SockJS('/ws');
const stompClient = Stomp.over(socket);
stompClient.connect({}, function (frame) {
    console.log('Connected: ' + frame);
});
```

### Топи для підписки
- `/topic/account-updates` - Оновлення рахунків
- `/topic/transaction-updates` - Нові транзакції
- `/topic/system-notifications` - Системні повідомлення

### Приклад повідомлення
```json
{
  "accountNumber": "123456789",
  "operation": "DEPOSIT",
  "amount": 100.0,
  "newBalance": 500.0,
  "timestamp": "2024-01-15T10:30:00",
  "message": "Account 123456789: DEPOSIT of 100.00 completed. New balance: 500.00"
}
```

## 🧪 Тестування

### Запуск тестів
```bash
cd backend
mvn test
```

### Типи тестів
- **Unit тести**: тести контролерів з MockMvc
- **Integration тести**: тести з H2 базою даних
- **Security тести**: тести авторизації та безпеки

### Тестова конфігурація
- H2 in-memory база даних
- Тестові профілі Spring
- Mockito для мокування залежностей

## 🐛 Вирішення проблем

### Проблема: "Cannot connect to Docker daemon"
**Рішення**: Запустіть Docker Desktop або Docker daemon

### Проблема: "Database connection failed"
**Рішення**: 
1. Перевірте, що PostgreSQL запущений: `docker-compose ps`
2. Перезапустіть контейнер: `docker-compose restart`

### Проблема: "Port 9000 already in use"
**Рішення**: 
1. Зупиніть процес на порту: `lsof -ti:9000 | xargs kill -9`
2. Або змініть порт в `application.yml`

### Проблема: "JWT token expired"
**Рішення**: Перезайдіть в систему через сторінку логіну

## 📊 Моніторинг

### Логи додатку
Логи зберігаються в файлі `backend/app.log` та виводяться в консоль.

### WebSocket статус
Перевірити статус WebSocket можна через `websocket-test.html` сторінку.

### База даних
Для перегляду даних в PostgreSQL:
```bash
docker exec -it <container_name> psql -U banking_user -d banking_db
```

## 🔮 Майбутні покращення

### Варіант 1: Окремий React фронтенд
- Створити React додаток на окремому порту
- Використовувати існуючий API
- Додати CORS налаштування для нового порту

### Варіант 2: Мобільний додаток
- React Native або Flutter додаток
- Використання існуючого REST API
- Push повідомлення через WebSocket

### Варіант 3: Розширені функції
- Кредити та позики
- Інвестиційні рахунки
- Міжнародні перекази
- SMS/Email повідомлення

## 📞 Підтримка

### Часті питання

**Q: Як скинути пароль?**
A: Наразі функція скидання пароля не реалізована. Створіть нового користувача або зверніться до адміністратора.

**Q: Чи можна змінити валюту рахунку?**
A: Ні, валюта рахунку встановлюється при створенні і не може бути змінена.

**Q: Як додати новий фронтенд (React, Vue, Angular)?**
A: Створіть новий проект на окремому порту та використовуйте існуючі API endpoints. Не забудьте додати CORS налаштування в `application.yml`.

**Q: Чи можна запустити без Docker?**
A: Так, але потрібно встановити PostgreSQL локально та налаштувати підключення в `application.yml`.

### Контакти
- **GitHub**: [Repository URL]
- **Email**: [Your Email]
- **Документація**: [Documentation URL]

---

**Версія**: 1.0.0  
**Останнє оновлення**: 2024-01-15  
**Автор**: [Your Name]